# BACKLOG — 今回の修理で扱わなかったもの

出所: `diagnosis/DIAGNOSIS.md` / `diagnosis/A_review.md`（2026-08-11 の診断後修理）

**この段階で、ここの項目を1つも実装しないこと。** 着手には前提条件の充足が要る。

各項目の見出し:

- **証拠**: 事実 / 推定 / 意見
- **従属**: Phase A-3 で指名した根本原因（観測の試行が第一級のデータでない）に
  従属していたか、独立か
- **今回**: 今回の修理で消えたか、残っているか

---

## B-01. 再試行の予算が「回数」であって「時刻」でない（H6）

- 証拠: **事実** — `weekly_deep_driver.py:888` `--max-attempts` 既定2
- 従属: **独立**
- 今回: **残っている**

8/8 は 12:10 に解除される使用量上限に対し 07:12 / 09:12 の2回で予算を使い切り、
解除の3時間前に 15/18 節で出荷された。取得層は「待てば直る失敗」を区別する
（`_net.py:126`）のに、執筆層に同じ区別が無い。

着手の前提条件:
- 使用量上限の解除時刻を、CLI の応答から機械的に取れることの確認
  （取れないなら「回数」から「時刻」へは移せない）
- 執筆層に触るため、判断層への波及がないことの確認

---

## B-02. 照合の独立集合が構造上ほぼ空（H7）

- 証拠: **事実**（コード: `reconciliation.py:388`）+ **推定**（「無人では脱出不能」）
- 従属: **独立**
- 今回: **残っている**

楽天CSVは模型の生成元、moomoo は残高ソースから除外済み。よって
「独立に検証できた」集合は設計上ほぼ常に空になり、無人実行では脱出経路が無い。

⚠️ A-2 の自己批判より: **「脱出不能」は調べずに書いた。**
無人で独立な残高ソースを得る手段（口座の日次メール、CSV自動配信、
第三者アグリゲータ）を検討した記録が無い。着手時はまずそこから。

着手の前提条件:
- 独立ソースの候補を最低3つ列挙し、それぞれの取得可能性を実測すること
- どれも不可なら「照合は原理的にできない」と設計に明記して、
  毎週同じ警告を出し続ける現状をやめる判断をすること

---

## B-03. 本番（無人）経路がまだ一度も検証されていない（H8）

- 証拠: **事実**（構造不一致・ログ欠如）+ **推定**（「対話で手作り」）
- 従属: **独立**
- 今回: **残っている**（今回の修理でも本番経路は走らせていない）

`週次PF分析_20260809_v3.md` は `build_sections()` の固定骨格と一致せず、
`weekly_deep.log` に生成記録が無い。修正が効いた成果物と、土曜に自動で出る
成果物は別物である。

着手の前提条件:
- **2026-08-15（土）07:12 の無人実行の結果を見る。** それが最初の実測になる
- 走らなかった場合、`WeeklyDeep` の `LastRunTime` と `output/weekly_deep.log` を確認

---

## B-04. `.claude/settings.json` のフックが相対パス

- 証拠: **事実** — `settings.json:29` `python scripts/hooks/guard_protected.py`
- 従属: **独立**
- 今回: **残っている**

matcher が `Write|Edit|MultiEdit|Bash` なので、シェルの作業ディレクトリが動くと
**全ツールが同時に死ぬ**。前セッションはこれで診断が中断し、DIAGNOSIS.md を
書けないままセッション再起動が必要になった（実際に起きた）。

着手の前提条件: なし（絶対パス化するだけ）。
**今回やらなかったのは、指示書の「診断書に載っていない改善をついでにやるな」に
従ったため。** 単独で1コミットにすること。

---

## B-05. 「実測」主張の割合という指標の作り直し

- 証拠: **事実**（Phase D で未達）
- 従属: 独立（測定設計の問題）
- 今回: **残っている**

Phase B で置いた「非空行100行あたりの『実測』出現数 ≥3.0」は、
Phase C-3 で実装した「分析を打ち切る」振る舞いと矛盾していた
（分析文が無い週に分析文の密度を要求していた）。

着手の前提条件:
- カバレッジ 70〜99% の週（＝打ち切らずに分析が走る週）の実物を1本得ること
- その実物で、A-3 の根本原因指名を再審すること（D_comparison.md §5 の反証条件）

---

## B-06. `with_retry` は症状を隠している疑い

- 証拠: **推定** — A-2 の自己批判より
- 従属: 独立
- 今回: **残っている**（今回は触っていない）

H1 の対処に入っている `wait_for_network()` は原因の除去だが、
同じコミットの `with_retry()` は禁じ手リストの「リトライ」そのもの。
両者が同居しているため、「原因を直した」という説明がリトライ追加の
隠れ蓑になりうる。

着手の前提条件:
- `wait_for_network()` だけを残して `with_retry()` を外した状態で、
  8/15 以降の無人実行が成功するかを実測すること
- 成功するなら `with_retry()` は不要。失敗するなら**別の原因が残っている**

---

## B-07. 診断そのものの未完了部分

- 証拠: **事実**（`DIAGNOSIS.md` §0 と §3 に明記）
- 従属: —
- 今回: **残っている**

診断は Phase 1（主張分類）と Phase 2（証拠収集）で止まっている。未実施:

- Phase 2 の実行プローブ（落ちた状態を再現して経路を確定する）
- Phase 3 設計突合 / Phase 4 削除候補 / Phase 5 ユーザー行動の変化 / Phase 6 総合
- 元の診断指示書が回収できず、**8件が「新規発見」か「与えられた仮説の言い換え」かを
  判定できていない**（A-2）

着手の前提条件: 元の診断指示書の本文を用意すること。

---

## B-08. Phase E（減算）— レポートの行数上限と静穏週の最小構成

- 証拠: **事実**（診断書 Q6-2 は未実施だが、768行という実測はある）
- 従属: 独立
- 今回: **未実行**

Phase D で目標未達（1件）だったため、指示書の
「Phase E は Phase D で目標を達成した場合にのみ実行する」に従い実行していない。

着手の前提条件:
- B-05 を解消し、Phase D を全指標達成で通すこと
