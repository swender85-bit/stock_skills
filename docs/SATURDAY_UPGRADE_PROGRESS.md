# 土曜週次レポート連携 — 実装進捗

---

## 完了報告（設計書 第8章のフォーマット）

### 実施段階

| 段階 | 内容 | commit |
|:---|:---|:---|
| S1 | 提案1 三点照合 ＋ 提案7 スナップショット記録 | `c13ccb5` |
| S2 | 提案8 情報量比例レポート（差分・反証条件） | `6cae9d7` |
| S3 | 提案3 手取り翻訳層 ＋ 提案9 現金/入金/注意予算 | `44c2d3a` → `28c9573` |
| S4 | 提案4 前方イベント脊椎 ＋ 7セクション固定骨格 | `186b031` |
| S5 | 提案5 執行監査 ＋ 横断的な縮退テスト ＋ 実地デモ | `09cc47e` |
| S6 | 提案2 限界寄与スクリーニング（因子基盤） | `36b9cb8` |
| S7 | 提案6 流動性制約付きストレステスト | `64ded39` |
| S8 | 提案10 模型監査（記録開始） | `318fedd` |
| — | パック生成の時間予算・工程別計測 | `f83b741` |
| — | 週次タスクの発火不具合修正 ＋ 資格情報衛生テスト | `505273d` |

### テスト結果

既存回帰 + 新規 = **4450 passed / 0 failed**（着手前は約3936）。新規約514件。

### 最終検証項目（設計書 第7章）

| # | 項目 | 担保 |
|--:|:---|:---|
| 1 | 照合を通過せずに分析が実行されない | `test_weekly_skeleton.py` 全kindで照合状態伝播を検証 |
| 2 | 同一銘柄が異なるPFで異なる限界スコア | `test_marginal.py` |
| 3 | 全乗り換え提案に税引後の損益分岐 | `test_tax_wiring.py`（what-if / adjust の両方） |
| 4 | 売却提案に入金充当の代替案 | `test_runway.py` / `test_tax_wiring.py` |
| 5 | 変化のない週にレポートが短くなる | `test_report_diff.py` / `test_weekly_skeleton.py` |
| 6 | 実行不能な推奨が警告される | `test_liquidity.py` |
| 7 | 混雑度が単独で売り推奨を作らない | `test_narrative.py` |
| 8 | 収入絶対額・認証情報が出力されない | `test_credential_hygiene.py` |
| 9 | opt-out で従来動作と一致 | `test_saturday_degradation.py` |
| 10 | Windows 実環境（TZ・パス・週末） | `test_saturday_degradation.py` |
| 11 | 祝日・年末年始・市場休場週 | `test_saturday_degradation.py` |
| 12 | 全API停止で古いデータを黙って使わない | `test_saturday_degradation.py` |

### ロールバック

```
git reset --hard 8ce46da
```

### ユーザーに残る手作業

- [ ] **`config/tax.yaml` の `meta.verified_as_of` に日付を入れる**
      （税率・NISA枠・手数料を確認したうえで。未確認だと毎回警告が出る）
- [ ] **QCOM の反証条件を登録する。** thesis に「撤退ライン約$158」と本人が
      書いているが、7/29 終値 $155.68 で既に割っている。条件として登録されて
      いないためシステムは検出できていない
- [ ] 孤児ポジション6件（評価額の約77%）に thesis か政策を付ける
      — 味の素 / ニトリHD / SOXL / TECL / TQQQ / FANG+
- [ ] `config/cashflow.yaml` に現金の目的と週次レビュー時間を書く
- [ ] （任意）moomoo で残高照会したい場合は `MOOMOO_ENABLED=on`

### 環境都合のフォールバック

- **moomoo**: US LV3 で米国株のみ。日本株・投信は scope 外として
  `unverified`（残高不明）に落ちる。幽霊扱いしない
- **GDELT**: 429 が続く回線のため多重ソース化（GDELT→Finnhub→yfinance）＋
  サーキットブレーカー＋時間予算180秒
- **Neo4j / TEI**: 未起動でも全機能が縮退動作する
- **日経先物**: 権限が無く取得できないため、月曜寄付の織り込みは
  「取得できず」と明示（材料なしとは書かない）

---

出典: `STOCK_SKILLS_土曜週次レポート連携設計書_Opus5.md`（S1〜S8）
着手: 2026-07-29 / バックアップコミット: **`8ce46da`**
ロールバック: `git reset --hard 8ce46da`

方針（ユーザー承認済み）:
- 実装範囲: **S1→S4 を完全実装**（中途半端にしない）。余力があれば S5 以降
- 実在残高: **moomoo 自動 ＋ 楽天CSV**
- 週次: **weekly_deep を7セクション固定骨格に作り変える**（旧版はフォールバックで残す）
- git: **段階ごとに commit ＋ push**（テスト全緑のときだけ push）

---

## S1 — 提案1（三点照合）＋ 提案7（スナップショット記録のみ） ✅ 完了

### 新規ファイル

| ファイル | 役割 |
|:---|:---|
| `src/data/brokers/base.py` | ブローカースナップショットの正規形。`available` / `as_of` / `scope` を必ず持つ |
| `src/data/brokers/rakuten_csv_broker.py` | 楽天CSV由来の実在残高。CSV原本を優先し、無ければYAMLへ縮退 |
| `src/data/brokers/moomoo_broker.py` | moomoo(OpenD) の米国株残高・約定履歴。scope は US のみ |
| `src/data/brokers/__init__.py` | `collect_snapshots()` / `merged_scope()` / `is_reconcilable()` |
| `src/core/portfolio/reconciliation.py` | 照合エンジン本体。幽霊/未記録/孤児/CA分類 |
| `src/output/reconcile_formatter.py` | 第1セクションの出力整形 |
| `src/core/research/narrative.py` | 物語量スナップショット記録（GDELT/Finnhub/yfinance 多重化） |
| `.claude/skills/stock-portfolio/scripts/portfolio_commands/reconcile.py` | `reconcile` サブコマンド |
| `tests/core/test_reconciliation.py` (28) / `tests/data/test_brokers.py` (16) / `tests/core/test_narrative.py` (17) | 受け入れ基準のテスト |

### 変更ファイル
- `run_portfolio.py` — `reconcile` サブコマンド追加
- `src/core/research/briefing_pack.py` — パックに `reconciliation` / `narrative` を追加
- `scripts/build_briefing_pack.py` — `--no-reconcile` / `--no-narrative`
- `.gitignore` — `data/narrative/` `data/reconciliation/`

### 設計判断（設計書から踏み込んだ点）

**1. 循環照合の検出（設計書に無い・実装して初めて判明）**

`config/weekly_holdings.yaml` は楽天CSVの取り込み出力であり、同時に週次レポートの入力（模型）でもある。
つまり素朴に実装すると「模型 vs その模型を作った元CSV」を突き合わせて必ず一致し、
**照合が常に成功する嘘の装置**になる。

対策: 読んだCSVが `source.file` と同一なら `circular=True` を立て、照合ステータスを
`ok` ではなく **`circular`（独立検証なし）** にする。`independently_verified=False` を
パックに載せ、週次レポート冒頭で警告する。実残高を本当に確かめるには新しいCSVの保存が要る。

**2. scope 外を幽霊にしない**

moomoo は US LV3 で米国株しか見えない。素朴に実装すると日本株・投信が全部
「口座に不在＝幽霊」になる。取得**成功した**ソースの scope の和集合だけを見て、
その外側は `unverified`（残高不明）に落とす。失敗したソースの scope は数えない。

**3. 自動補正は分割/併合だけ**

売買の記録漏れを自動で辻褄合わせすると「どの取引を記録し忘れるか」という
記録習慣の弱点が見えなくなる。`apply_corporate_actions()` は `auto_fixable` な
コーポレートアクションのみを模型に反映する。

**4. 物語量ソースの多重化（設計書は GDELT 単独を想定）**

GDELT はこの回線から 429 が連続する（実測）。単一ソースに賭けると記録が途切れ、
**混雑度の分母が永久に欠ける**。GDELT → Finnhub → yfinance の順に試し、
取れたものを `volume_source` 付きで記録する。

重要: `crowding()` は**同一ソース同士でしか比を作らない**。GDELT の全世界記事数と
Finnhub の企業ニュース件数は母集団が違い、比にすると無意味な数字が出る。
さらに GDELT 連続失敗時はサーキットブレーカーで以後スキップし、週次バッチが止まらないようにする。

### 現状の実測結果（本人PF）

```
判定: 🟡 独立検証なし
口座: 9銘柄 / 模型: 9銘柄 / 一致 9 / 差分 0
⚠️ 孤児ポジション 6件 — 味の素・ニトリ・SOXL・TECL・TQQQ・FANG+
```

→ **保有の大半に thesis も政策も無い。** これは設計書が「どのシステムも検出しない」と
書いた通りの状態。S4 で政策カバレッジの穴として毎週突きつける。

### やることリスト（ユーザー側の手作業）
- [ ] 楽天証券から**新しいCSV**を保存 → `python scripts/import_rakuten_csv.py`
      （これをやるまで照合は `circular` のまま＝独立検証にならない）
- [ ] 孤児6件に thesis か政策を付ける（`/investment-note save --type thesis` か
      `python scripts/manage_policy.py add`）
- [ ] moomoo で残高照会したい場合は `MOOMOO_ENABLED=on`。取引照会に
      unlock が要る環境では `MOOMOO_TRADE_PWD_MD5` を環境変数で設定

---

## S2 — 提案8（情報量比例・差分レポート） ✅ 完了

### 新規ファイル

| ファイル | 役割 |
|:---|:---|
| `src/core/portfolio/report_diff.py` | 週次スナップショット・前週差分・4週/13週累積差分・情報量判定 |
| `src/core/portfolio/falsification.py` | thesis の反証条件を点検（「信念の変化」を見る層） |
| `src/output/weekly_diff_formatter.py` | 判定ブロック・信念セクション・折り畳み表示 |
| `tests/core/test_report_diff.py` (21) / `tests/core/test_falsification.py` (24) | 受け入れ基準のテスト |

### 変更ファイル
- `src/data/note_manager.py` — `save_note(..., falsification=...)`（thesis 限定）
- `manage_note.py` — `--falsification` 追加。**測定不能な条件は保存前に拒否**
- `briefing_pack.py` — `falsification` / `week_diff` / `cumulative_diff` / `information`
- `.gitignore` — `data/weekly_snapshots/`

### 設計判断

**1. 反証条件は測定可能でなければ受け付けない**

「業績が悪化したら」は保存時に弾く。点検できない条件を許すと、書いた気になるだけで
週次の点検対象が増えない。使える指標は政策台帳の `MEASURABLE_METRICS` を継承し、
テーゼ検証向けに `revenue_growth` / `roe` / `earnings_growth` 等を追加した
（政策側の集合は変えていない＝非破壊）。

**2. 複数条件は OR**

「全部壊れないと反証を認めない」は、間違いを認めない構造そのもの。1つでも成立したら反証。

**3. 「指標が取れなかった」を「抵触なし」と言わない**

`unknown` を独立の状態として持ち、レポートで「未点検」と明示する。

**4. 静穏週の判定を厳しくした（設計書から踏み込んだ点）**

素朴に実装すると、照合が `circular`（独立検証なし）でも孤児が6件あっても
「静穏週・何もしなくてよい」と出てしまう。以下を要対応に数えるようにした:

- 照合が `unreconciled` / `circular`（独立検証できていない）
- 孤児ポジションの存在
- 反証条件が未定義 / 点検不能な thesis

**5. 緩慢な変化の第二の網**

前週比だけだと「毎週2%ずつ下落」が週次閾値5%に永久に引っかからない。
4週/13週の累積差分を併走させ、`slow_drift` として別枠で出す。
N週前が無いときは直近で代用せず「蓄積中」と言う（代用すると検出が壊れる）。

### 現状の実測結果（本人PF）

```
■ 今週の判定：要対応週（要対応 3件）
  1. 【信念】反証条件が未定義の thesis 2件（MDT / QCOM）
  2. 【照合】残高の独立検証ができていません
  3. 【照合】孤児ポジション 6件（評価額の 77.6%）
```

→ **評価額の 77.6% が「なぜ持っているか未記述」。** 設計書が予告した通りの状態。

### やることリスト（ユーザー側）
- [ ] MDT / QCOM の thesis に反証条件を追加
      例: `manage_note.py save --symbol QCOM --type thesis --content "..." --falsification "revenue_growth < 0"`
- [ ] 孤児6件（味の素・ニトリ・SOXL・TECL・TQQQ・FANG+）に thesis か政策を付ける


## S3 — 提案3（手取り）＋ 提案9（現金・入金・注意予算） ✅ 完了

### 新規ファイル

| ファイル | 役割 |
|:---|:---|
| `config/tax.yaml` | 税率・NISA枠・手数料・為替スプレッド（ハードコード禁止） |
| `config/cashflow.yaml` | 積立額・入金推定パラメータ・現金の目的・注意予算 |
| `src/core/portfolio/tax.py` | switching_hurdle / NISA / 含み損の税務価値 / 税務状態 |
| `src/core/portfolio/runway.py` | 入金推定 / ランウェイ / 資金調達の4選択肢 / 注意予算 |
| `src/output/constraint_formatter.py` | 第4セクション「制約」の出力 |
| `tests/core/test_tax.py` (49) / `test_runway.py` (37) / `test_tax_wiring.py` (19) | 受け入れ基準 |

### 変更ファイル
- `portfolio_simulation.py` — what-if の売却に税引後の手取り・損益分岐を**必須添付**
- `simulate_formatter.py` — 税引前/税引後を並記。判断に使うのは税引後の差額
- `adjustment_advisor.py` — SELL/SWAP に `tax_view` と `funding_alternative` を付与
- `adjust_formatter.py` — 「税引後の再評価」節を追加
- `briefing_pack.py` — パックに `constraints`（税務状態・ランウェイ・注意予算・含み損の価値）
- `.claude/rules/plan-check.md` — Review に **Tax Checker** を追加（4エージェント化）

### 設計判断（設計書から踏み込んだ点）

**1. 「43週待て」は計画ではない**

素朴に実装すると、入金ペースに対して目標額が大きすぎる場合でも「待つ」が
推奨1位になり、先延ばしを最善手として提示してしまう。ランウェイ（12週）の
2倍を超える待機は `realistic=False` にして推奨から外す。

**2. `resize`（取得規模を落とす）を選択肢に追加**

「テーゼが生きている銘柄を売る」と「1年待つ」しか無いとき、どちらかを推奨するのは誤り。
買う量を減らせばよい。`thesis_alive=True` の売却は最下位（rank 6）にして、
「売る理由がない」と書きながら売却を推奨する矛盾を防いだ。

**3. 実現損益が不明なら 0 にしない**

売買履歴が無いとき 0 を返すと「利益が無い＝損益通算する相手がいない」と誤読される。
`None` を返し、「不明」と「ゼロ」を区別する。相殺相手が無い場合は
「約0円の税が消えます」という情報ゼロの行を出さない。

**4. NISA使用額は推定だと明示する**

保有データに取得日が無いため「当年に使った枠」は原理的に出せない。
`reliable=False` を立て、レポートでは「（推定）」タグと注意書きを付ける。
確定値として出すと、残枠を過大/過小に見せて枠の消滅を招く。

**5. 収入情報の扱い（設計書の内部矛盾への対処）**

設計書 提案9-⑧ は「レポート出力には絶対額を書かない」と述べるが、⑥の出力例では
週次投資可能額を絶対額で出している。矛盾するので次のように分けた:

- **Neo4j へは絶対額を書かない** — `to_graph_safe()` が比率のみに落とす（テストで強制）
- **ローカルのレポートには出す** — 出さないと「N週待てば買える」が言えず機能が死ぬ
- `config/cashflow.yaml` の `privacy.disclose_absolute_amounts: false` で抑制可能

### 実測結果（本人PF・2026-07-30 CSV反映後）

```
■ 手取り状態（2026年分）
  当年実現損益   未記録（売買履歴から自動集計できませんでした）
  NISA成長投資枠（推定） 残 ¥2,212,293 / ¥2,400,000（使用率 7.8%）
  NISAつみたて枠（推定） 残 ¥100,000 / ¥1,200,000（使用率 91.7%）
  → 年内残り22週。推定ベースで約231万円の非課税枠が未使用のまま消滅
■ 資金ランウェイ
  現金残高 ¥10,275（評価額比 0.1%）/ 週次投資可能額 約¥11,538
  ⚠️ 目的が割り当てられていない現金
■ 注意予算
  保有10銘柄 / 1銘柄4.5分 / ⚠️ 6銘柄が孤児 → 実質監視は4銘柄
```

### やることリスト（ユーザー側）
- [ ] **`config/tax.yaml` の内容を確認して `meta.verified_as_of` に日付を入れる**
      （税率・NISA枠・手数料。未確認だと毎回警告が出る）
- [ ] NISA の実際の当年使用額を証券会社で確認（推定値は当てにしない）
- [ ] `config/cashflow.yaml` の `cash.purposes` に現金の目的を書く
- [ ] 週次レビュー時間 `attention.weekly_review_minutes` を実態に合わせる

---

## 三点照合の実測更新（2026-07-30）

7/30 のCSVを**取り込む前に**照合したところ、**初めて独立検証が成立し完全一致**した:

```
判定: ✅ 一致
口座: 9銘柄 / 模型: 9銘柄 / 一致 9 / 差分 0
```

7/23 時点の模型と 7/30 の実口座に差分ゼロ = 取り込み漏れなし。
その後CSVを取り込み済み（為替 ¥162.92/USD に更新、保有構成の変化なし）。

**運用上の学び**: 独立照合を成立させるには、**新しいCSVを取り込む前に** reconcile を
実行する。取り込み後に実行すると模型の生成元と同一データになり `circular` になる。
この案内をエラーメッセージに反映済み。

## S4 — 提案4（前方イベント脊椎）＋ 政策台帳の合流 ✅ 完了

### 新規ファイル

| ファイル | 役割 |
|:---|:---|
| `src/data/yahoo_client/events.py` | 決算予定日・配当落ち日の取得（yahoo_client 経由） |
| `src/core/risk/forward_events.py` | 翌週カレンダー / イベント集中度 / 政策カバレッジの穴 / 配当落ち分離 / 月曜寄付 / 日程変更検出 |
| `src/output/forward_formatter.py` | 第3セクション「前方イベント」の出力 |
| `tests/core/test_forward_events.py` (36) / `tests/data/test_yahoo_events.py` (18) | 受け入れ基準 |

### 設計判断
- **イベント集中度は評価額加重**。小さい保有3件と主力1件では同じ「3件」でもリスクが違う。
  折り畳んだ小さいイベントも集中度には算入する（見た目で消えても存在する）。
- **同一銘柄を複数口座で持つと決算が二重に出る**バグを実装中に発見。銘柄単位に畳んで
  評価額比を合算するよう修正（味の素が 1.0% と表示され、実際は 11.4% だった）。
- **日本株は「権利付最終日」に変換**（権利落ち日の前営業日）。買うならその日までなので
  行動に関係するのはこちら。祝日は考慮できないので caveat を明示。
- 先物・ADR は**予測ではなく市場の織り込み**。的中率は記録しない（予測装置に見せない）。
- 前週カレンダーを週次スナップショットに保存し、決算日変更を検出できるようにした。

---

## 週次レポートの7セクション固定骨格化 ✅ 完了

`.claude/prompts/weekly_deep.md` を全面改訂し、`weekly_deep_driver.py` の
`build_sections` / `slice_pack` / `assemble` を設計書 第3章の骨格に作り変えた。

```
0. 判定 → 1. 照合 → 2. 信念の変化 → 3. 前方イベント → 4. 制約
       → 5. 機会 → 6. 事前決定 → 7. 監査 → 8. 前提と限界
```

### 設計判断
- **第0原則を仕様の冒頭に据えた**: 土曜は完全情報×執行不能の特異点。
  「いま買うべき銘柄」を出力してはならない。出力形式は**条件付き政策**。
- **照合の状態を全節に渡す**（`reconciliation_status`）。未照合のまま
  下流の節が数値を確定値として語るのを防ぐ。テストで全 kind について保証。
- **静穏週は銘柄別の節を作らない**。分量をスケジュールでなく情報量に比例させる。
  骨格の節自体は落とさない（折り畳みは削除ではない）。
- ヘッダに三点照合の判定と今週の判定を出す。初回で点検0項目のときは
  「前週比較なし（初回）」と書く（動いていないと誤読させない）。

### 実地デモ（2026-07-30 実データ・2節生成）

生成された第1節・第2節の実出力から、設計意図どおりに動いていることを確認:

- 照合節が**循環照合を自力で見抜き**、「CSVの内容がCSV自身と一致したという以上のことを
  証明していない」と書いた上で、独立照合への切り替え手順を提示した。
- 孤児6件（評価額の77.0%）を表で提示し、「保有の77%について損切りも利確も
  判断基準が存在しない」と意味に翻訳した。
- 信念節が「反証条件0件成立」を**「テーゼが持ちこたえた」ではなく
  「点検する対象が存在しなかった」**と正しく読み替えた。

### 🔴 実データで判明した重要事項（ユーザー要確認）

**1. QCOM が本人の記載した撤退ラインを既に割っている**

thesis メモに「撤退ライン約$158（取得-15%）」と本人が書いているが、
7/29 終値は **$155.68** で既に割り込んでいる。反証条件として登録されていないため、
システムは今週これを点検していない。**S2で作った反証条件を登録すれば自動検出される。**

**2. 半導体3xが週間で暴落している（データ異常ではなく実物）**

SOXL は 7/22 $160.99 → 7/29 $91.99（週間 **-42.9%**）。TECL -21.5%、TQQQ -17.8%。
総資産は ¥23.4M → ¥18.6M。運用ルール上「損切りなし・下落で積み増し」だが、
**現金クッションは ¥10,275 しかなく積み増し余力がほぼ無い**（提案9の資金ランウェイが
まさにこの状態を毎週突きつける）。

---

## S5 — 提案5（執行監査・決定生存率） ✅ 完了

### 新規ファイル
| ファイル | 役割 |
|:---|:---|
| `src/core/portfolio/execution_audit.py` | 判断↔約定のマッチング / 決定生存率 / 執行ショートフォール / 成績の分離 |
| `tests/core/test_execution_audit.py` (33) | 受け入れ基準 |

配線: `briefing_pack` の `execution_audit`（第7セクション「監査」）、
`run_portfolio.py review` の末尾、`weekly_deep.md` の監査節仕様。

### 設計判断

**1. 「約定履歴が取れない」を「執行率0%」と言わない（実データで踏んだ）**

実装直後に review を回したら「生成された判断 46件 / 実際に執行された判断 0件
（決定生存率 0.0%）」と出た。だが実際は moomoo が無効で**約定履歴が取れていない**だけ。
これは設計書の核心原則（取得失敗を結果と混同しない）そのものの違反だったので、
`executions_available` を導入して「測定できていない」と言うよう修正した。テストで固定済み。

**2. ショートフォールの符号を揃える**

買いは高く買うほど不利、売りは安く売るほど不利。符号を揃えないと買い売り混在で
摩擦が相殺されて消える。

**3. トーンの設計をコードに埋め込む**

決定生存率が低いとき、メッセージは「あなたの怠慢ではなく、**提案の形式が
実行可能でなかった**証拠として扱います」と書く。責任の所在をシステム側に置く
（設計書 提案5-⑧）。未執行理由は常に「推定です」と明示する。

---

## 実地デモ（設計書 第8章の要求事項） ✅ 完了

2026-07-30 の実データで**全18節を無人生成**し、vault まで同期した。

- 出力: `output/週次PF分析_20260730.md`（765行）
- vault: `C:/Users/swend/iCloudDrive/swender/投資記録/週次PF分析_20260730.md`
- ログ: `output/weekly_deep_demo.log`

### 固定骨格が正しい順序で出力されたことの確認

```
## 0. 今週の判定        (19行目)
## 1. 照合              (49行目)
## 2. 信念の変化        (84行目)
## 3. 前方イベント      (118行目)
## 4. 制約              (177行目)
## 5. 機会              (259行目)
## 6. 事前決定          (481行目)
## 7. 監査              (653行目)
## 8. 前提と限界        (694行目)
```

### 設計意図どおりに機能した点

- 照合節が**循環照合を自力で見抜き**、独立照合への切り替え手順を提示した。
- 信念節が「反証条件0件成立」を**「点検する対象が存在しなかった」**と読み替えた。
- 判定節が、システムの自動検出（5件）に加えて
  **「QCOM が本人記載の撤退ライン $158 を既に割っている」**を執筆中に発見し、
  「記録の欠落が実損の検出漏れに変わった最初のケース」と位置づけた。
- 事前決定節が **D-0「今週は売買しない（deliberate_inaction）」** を
  明示的な意思決定として記録し、味の素の税引後損益分岐 3.6% を計算した上で
  「-15% のギャップに対して『売らない』と事前に決めておくことに意味がある —
  決めていなければ、その朝に決めることになるからだ」と書いた。
- **銘柄の買い推奨は1件も出力されなかった**（第0原則が守られた）。

---

## S6 — 提案2（限界寄与スクリーニング・因子基盤） ✅ 完了

設計書が「最も価値が高いが最も重い」と位置づけた提案。

### 新規ファイル
| ファイル | 役割 |
|:---|:---|
| `src/core/exposure.py` | 5因子の自前構築・重回帰・PF集約・因子双子・ストレス相関 |
| `src/core/screening/marginal.py` | 補完係数 / 限界スコア / ルックスルー |
| `src/core/screening/marginal_bridge.py` | screen-stocks への配線（縮退込み） |
| `src/output/marginal_formatter.py` | 因子偏り・限界スコア順の出力 |
| `tests/core/test_exposure.py` (33) / `test_marginal.py` (29) | 受け入れ基準 |

変更: `run_screen.py` に `--standalone`（従来動作に戻す）、
`.claude/rules/screening.md`、`plan-check.md` の Devil's Advocate に因子双子・
通貨の二重ロング・平時相関の過信を追加。

### 実装中に判明した問題と対処

**1. 日本株を米国因子に同日で当てると感応度が消える**

日本株の当日終値は**前日の米国市場**を織り込んで始まる。同日で回帰すると
時差のぶん相関が消える。アジア市場の銘柄には米国因子を1日ラグさせるようにした
（`needs_lag()` / `_shift_one_day()`）。

**2. 「1因子でも符号反転 → 全体を不安定扱い」が粗すぎた**

最初の実装では、原油の符号が反転しただけで半導体感応度の評価まで割り引かれ、
**補完係数が全候補で 1.0 近辺に潰れて機能しなかった**（NVDA 91→85.3 程度）。
`unstable_factors` を因子単位で返し、該当因子の寄与だけを割り引くよう修正。
修正後は NVDA 91→74.7（半導体・市場を増幅するため減点）と正しく効いた。

**3. R² が低い銘柄はβの符号自体がノイズ**

日本の中小型株は5因子で R²=0.13〜0.15 程度。数字が出たことを「推定できた」と
誤読させないため、R² < 0.20 なら全因子を不安定扱いにする。

### 実データでの出力（本人PF）

```
PF betas: market +1.21 / usdjpy -0.22 / rates +0.00 / oil +0.03 / semis +1.56
 ⚠️ 半導体感応度 +1.56 — 半導体シクリカルにほぼ全面的に連動する。

候補（限界スコア順）
  NVDA    単独 91.0 → 限界 74.7（係数0.821）市場ベータと半導体を増幅
  AMD     単独 82.0 → 限界 69.0（係数0.842）
  8306.T  単独 74.0 → 限界 68.4（係数0.924）
  KO      単独 68.0 → 限界 68.1（係数1.001）唯一の補完側
```

**単独スコアでは 91 vs 68 で23点差だった NVDA と KO が、保有考慮後は 6.6点差**に縮まる。
このPFは半導体に全面連動しているため、半導体株の追加は分散を改善しない。

---

## S7 — 提案6（流動性制約付きストレステスト） ✅ 完了

### 新規ファイル
| ファイル | 役割 |
|:---|:---|
| `src/core/risk/liquidity.py` | 解消所要日数 / 4層プロファイル / 推奨の実行可能性 / 暴落時の買い余力 / 口座区分の下方非対称 |
| `src/output/liquidity_formatter.py` | 流動性セクションの出力 |
| `tests/core/test_liquidity.py` (33) | 受け入れ基準 |

配線: `run_stress_test.py` の末尾、`briefing_pack` の `constraints.liquidity`
（＝第4セクション「制約」）、`weekly_deep.md` の制約節仕様。

### 設計判断
- **参加率は出来高の10%**（保守的）。上げると「売れるはず」の楽観が入り込む。
- **ストレス時の出来高は単一の値に決めない**。楽観/中庸/保守の3通りを併記する
  （実際のストレス時出来高は事前に分からないため）。
- **`unknown` を `immediate` に混ぜない。** 出来高が取れないことを「流動性が高い」
  と扱わない。
- **ティッカーを持たない保有（投信）を黙って落とさない。** 落とすと合計が100%に
  ならず「残りは流動的」と誤読される。`unmeasurable_pct` として明示する。
- **`0.0日` と書かない。** 0.05日未満は「即日」と表示する（`format_days`）。
- 流動性は**却下条件ではなく警告とサイズ制約**。小型株投資を禁止しない。

### 実データ
このPFは保有株数が出来高比で極小なため全て即時解消可能（閉じ込め資本ゼロ）。
一方 **現金比率 0.0%** で「暴落時に買う」計画が成立しない状態が明示された。

---

## 三点照合の実測更新（2026-07-31）

7/31 22:49 のCSVを**取り込む前に**照合し、**2週連続で独立検証が成立・差分ゼロ**。

```
判定: ✅ 一致 / 口座 9銘柄 / 模型 9銘柄 / 一致 9 / 差分 0
```

取り込み済み（為替 ¥159.37/USD に更新、保有構成の変化なし）。
**「新CSVは取り込む前に reconcile」の運用が定着した。**

---

## S8 — 提案10（模型監査） ✅ 完了（記録開始・採点は26週後から有効）

### 新規ファイル
| ファイル | 役割 |
|:---|:---|
| `src/core/risk/model_audit.py` | 週次予測の生成 / (予測,実現) の記録 / 採点 / 欠落因子の仮説 / 下流への信頼度伝播 |
| `tests/core/test_model_audit.py` (26) | 受け入れ基準 |

配線: `briefing_pack` の `model_audit`（第7セクション「監査」）、`weekly_deep.md`。
`src/core/exposure.py` に `weekly_factor_moves()` を追加。

### 設計判断

**1. 因子の週次変化を専用に計算する（指数ウォッチを流用しない）**

指数ウォッチの `percent_change` は**日次**。実現リターンは週次なので、
そのまま予測に使うと予測と実現が別の単位になる。`weekly_factor_moves()` で
直近5営業日の日次リターンを複利で積む。

**2. 26週未満では結論を出さない**

週次データは年52点しかない。少数で結論を出すと偶然を模型の欠陥と誤認する。
`available=False` と「データ蓄積中（N/26週）」だけを返す。

**3. 系統的バイアスは統計的検定を伴って報告する**

片側二項検定（scipy 不要の自前実装）。p < 0.01 のときだけ「偶然としては
起こりにくい偏り」と書く。有意でない乖離は報告しない。

**4. 因子は自動追加しない**

欠落因子は `suggest_missing_factor()` が**仮説として**提示するだけ。
採用は人の判断に委ねる（設計書 提案10-⑧）。

**5. 記録は今週から始める**

26週の蓄積が前提だが、記録を始めなければ26週後も測れない。
分析が失敗しても記録は残るよう、**記録を分析より先に**実行する。

### 実データ（2026-07-31 初回記録）

```
週次因子変化: market +0.27% / usdjpy -2.71% / rates +0.92% / oil -4.57% / semis -3.44%
予測 -5.13%（うち semis 寄与 -5.46%）
実現 -4.65%
誤差 +0.49%
採点: データ蓄積中（0/26週）
```

因子模型はPFの週次変動をよく捉えている（半導体β1.59 × 半導体 -3.44% が支配的）。
**この (予測, 実現) ペアの蓄積が今週から始まった。**

---

## 全段階（S1〜S8）完了

| 段階 | 提案 | 状態 |
|:---|:---|:---|
| S1 | 三点照合 ＋ 物語量スナップショット記録 | ✅ |
| S2 | 情報量比例レポート（差分・反証条件） | ✅ |
| S3 | 手取り翻訳層 ＋ 現金/入金/注意予算 | ✅ |
| S4 | 前方イベント脊椎 ＋ 7セクション固定骨格 | ✅ |
| S5 | 執行監査・決定生存率 | ✅ |
| S6 | 限界寄与スクリーニング（因子基盤） | ✅ |
| S7 | 流動性制約付きストレステスト | ✅ |
| S8 | 模型監査（記録開始） | ✅ |

**提案7の分析部分（混雑度）**のみ、設計どおり基準点の蓄積待ち。
スナップショット記録は S1 から動いており、比較できるようになり次第
自動で分析が有効になる（`narrative.crowding()`）。
