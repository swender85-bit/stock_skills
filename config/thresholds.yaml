# config/thresholds.yaml
# Centralized threshold values for screening, health-check, and portfolio modules.
# Each section maps to a logical domain; keys are referenced via
#   from src.core._thresholds import th
#   th("health", "rsi_prev_threshold", 50)
# The third argument (default) must always match the value here so that
# the system works even when the YAML file is missing.

# ---------------------------------------------------------------------------
# Health check (src/core/health_check.py)
# ---------------------------------------------------------------------------
health:
  sma_approaching_gap: 0.02        # SMA50 within 2% of SMA200
  rsi_prev_threshold: 50           # Previous RSI level for drop detection
  rsi_drop_threshold: 40           # Current RSI level indicating a drop
  cross_lookback: 60               # Trading days to scan for cross events

  # Small-cap allocation rules (KIK-438)
  small_cap_warn_pct: 0.25       # 小型株比率 > 25% で警告
  small_cap_crit_pct: 0.35       # 小型株比率 > 35% で危険
  small_cap_cross_lookback: 30   # 小型株のクロスルックバック（通常60→30）

  # Long-term suitability (KIK-371)
  lt_roe_high: 0.15
  lt_roe_low: 0.10
  lt_eps_growth_high: 0.10
  lt_dividend_high: 0.02
  lt_per_overvalued: 40
  lt_per_safe: 25

# ---------------------------------------------------------------------------
# Small-cap classification thresholds (KIK-438/574)
# Region-specific small-cap market cap thresholds (local currency).
# ---------------------------------------------------------------------------
small_cap:
  jp: 100000000000      # 1000億円
  us: 1000000000        # $1B
  sg: 2000000000        # SGD 2B
  th: 30000000000       # THB 30B
  my: 5000000000        # MYR 5B
  id: 15000000000000    # IDR 15T
  ph: 50000000000       # PHP 50B
  hk: 10000000000       # HKD 10B
  kr: 1000000000000     # KRW 1T
  tw: 30000000000       # TWD 30B
  large_cap_multiplier: 5   # 大型株 = 小型株閾値 × 5

# ---------------------------------------------------------------------------
# Return estimation (src/core/return_estimate.py)
# ---------------------------------------------------------------------------
estimate:
  return_cap: 0.30                 # Max annualized return cap (+/-30%)
  min_spread: 0.05                 # Minimum spread for scenarios

# ---------------------------------------------------------------------------
# Technicals / pullback screening (src/core/screening/technicals.py)
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# Contrarian screener (src/core/screening/contrarian_screener.py)
# ---------------------------------------------------------------------------
contrarian:
  prefilter_fifty_day_max: 0.05    # Skip stocks with 50-day avg change > +5%
  prefilter_52wk_high_min: -0.05   # Skip stocks within 5% of 52-week high

# ---------------------------------------------------------------------------
# Technicals / pullback screening (src/core/screening/technicals.py)
# ---------------------------------------------------------------------------
technicals:
  pullback_min: -0.20              # Pullback lower bound (from recent high)
  pullback_max: -0.05              # Pullback upper bound
  rsi_reversal_lo: 25.0            # RSI reversal zone lower
  rsi_reversal_hi: 50.0            # RSI reversal zone upper
  rsi_depth_lo: 25.0               # RSI depth bonus lower
  rsi_depth_hi: 35.0               # RSI depth bonus upper
  bb_proximity_mult: 1.02          # Bollinger lower-band proximity multiplier
  volume_surge_ratio: 1.2          # Volume ratio threshold for surge bonus
  bounce_signal_min: 40.0          # Minimum bounce score to signal
  # Per-signal score weights
  score_rsi_reversal: 40.0
  score_rsi_depth: 15.0
  score_bb_proximity: 25.0
  score_volume_surge: 10.0
  score_price_reversal: 10.0

# ---------------------------------------------------------------------------
# 長期金利ゲート (src/core/policy/evaluator.py) — 改善6
#
# レバレッジETFは実質ロング・デュレーション資産であり、効くのは短期金利ではなく
# **長期金利**。直近のFOMCでは:
#
#     2年債   ▲4bp    （政策金利予想は緩和）
#     30年債 +12〜14bp（19年ぶり高水準）
#     → 政策金利予想が緩んだ日に、株は大幅安
#
# 短期金利予想だけを見ていると、この日を「緩和」と誤判定する。
#
# ⚠️ **5.50% に理論的根拠はない。** 現在値からの距離で置いた暫定値であり、
#    rationale: provisional を付けてある。3ヶ月後に実測で見直すこと。
#    条件を増やすほど「永遠に買えない」リスクが上がるので、この条項が
#    実際に何回投入を止めたかは deviation.py の逸脱監査で追跡する。
# ---------------------------------------------------------------------------
rates:
  rationale: provisional        # 暫定値。実測で見直すまで根拠は「現在値からの距離」のみ
  set_on: 2026-08-05
  review_after: 2026-11-05
  ust30y_warning: 5.50          # これを超えたらレバETFへの新規投入を見送る
  ust10y_warning: 5.00
  ust30y_spike_1m: 0.50         # 1ヶ月で+50bp の急騰でも見送る
  # 拒否条項を適用する対象。1倍株には適用しない（デュレーション感応度が違う）
  gate_min_leverage: 2

# ---------------------------------------------------------------------------
# Theme balance (src/core/health/theme_balance.py) (KIK-605)
# ---------------------------------------------------------------------------
theme_balance:
  max_theme_weight: 0.20       # 同一テーマのPFウェイト上限
  max_theme_stocks: 3          # 同一テーマの銘柄数上限
  per_warn_multiplier: 2.0     # セクター中央PERの何倍で警告
  fng_caution_threshold: 80    # F&G スコアがこれ以上でテーマ追加購入に警告
  theme_stale_days: 90         # テーマ鮮度の日数閾値

# ---------------------------------------------------------------------------
# Data quality gate (src/core/research/briefing_pack.py, scripts/weekly_deep_driver.py)
# 2026-08-11 の修理で導入。ハードコードしていた 0.7 をここへ移した。
# ---------------------------------------------------------------------------
data_quality:
  min_price_coverage: 0.7      # これを下回るパックからレポートを書かない
  abort_report_max_lines: 80   # 打ち切り時に出す欠測報告の行数上限
